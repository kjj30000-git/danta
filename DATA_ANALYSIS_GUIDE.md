# 자동매매 실행 결과 데이터 분석 지침

이 문서는 매 실행 결과를 같은 순서와 기준으로 검토하기 위한 공통 지침이다. 버전별 코드·인수인계서의 전략 정의가 다르면 해당 버전의 최신 인수인계서와 실제 실행 코드 정의를 우선 확인하고, 불일치를 보고서에 적는다.

## 1. 분석 전 자료 확인

### 기준 자료

다음 자료를 직접 확인한다.

1. 프로젝트 `README.md`: 장기 원칙과 데이터 분석 원칙
2. `CURRENT.md`: 현재 기준 버전과 실행 상태
3. 대상 버전의 최신 최종 인수인계서
4. 실행 당시 실제로 사용한 전체 코드
5. `data/버전(실행일자)/`의 모든 결과 CSV·JSON
6. 있으면 시스템 로그, 시작 오류 화면, Telegram 알림 상태 기록

코드의 계획 출력 목록과 실제 업로드된 파일 목록을 비교한다. 신규 전략의 SIGNAL/평가 원장이 빠졌다면 이는 단순 누락으로 치부하지 말고, 전략 평가 신뢰도 제한으로 표시한다.

### 버전별 핵심 결과 파일 예시

실제 코드에서 파일명을 확인하며, 이름이 다른 버전은 해당 버전의 파일을 사용한다.

| 분석 목적 | v2.3.0 예시 |
|---|---|
| 종목 탐지 및 기본 신호 | `scanner_signals_v230.csv` |
| 실행·API·WebSocket·부하 로그 | `scanner_system_v230.csv` |
| paper 거래 결과 | `paper_trades_v230.csv` |
| 진입 거부·스킵 사유 | `paper_entry_decisions_v230.csv` |
| 연구 진입 후 가격 경로 | `paper_entry_path_v230.csv` |
| 후보·봉·queue metrics | `pullback_metrics_v230.csv` |
| 후보 종료 상태·알림 outbox | `pullback_state_v230.json` |
| 신규전략 SIGNAL/FILL 원장 | `pullback_structure_signals_v230.csv` |
| SNAPSHOT horizon별 판정 | `pullback_snapshot_evaluations_v230.csv` |
| paper trade 선택 정책 결과 | `paper_policy_research_v230.csv` |
| 청산 후 가격 경로 | `paper_post_exit_v230.csv` |

생성되지 않은 파일 종류를 임의로 만들어 채우지 않는다. 생성되지 않은 이유를 확인할 수 없으면 “확인 불가”로 남긴다.

## 2. 데이터 무결성 점검

분석 계산 전에 다음을 수행한다.

- CSV 인코딩/BOM, 구분자, 따옴표, 날짜·시간 컬럼을 확인한다.
- 같은 파일의 반복 header, 분할 파일 중복, 동일 SHA/반복 업로드를 확인한다.
- 분할본이면 part 순서에 맞춰 병합하고, 반복 header는 제거한다.
- 종목코드와 trade_id는 문자열로 보존한다. 날짜·가격 값은 원본을 덮어쓰지 않는다.
- 필수 컬럼 존재, 행 수, 비어 있는 핵심 필드, 중복 키를 검증한다.
- 버전·날짜·전략명·실행 모드가 일치하는지 확인한다.
- 상태 JSON은 schema/date를 확인하고 복원해 후보 수, mode별 signals, pending, fill keys, outbox, candidate state를 확인한다.
- 실행 로그·CSV·JSON 간 counts와 시각을 교차검증한다.
- metrics의 누적값은 매 샘플의 차이가 단조롭게 맞는지, 컬럼이 잘못 맞물리지 않았는지 확인한다.
- 어떤 분모도 확인하기 전에는 승률·결측률·오류율을 보고하지 않는다.

## 3. 행 수와 독립 표본 구분

가상 거래 파일의 각 TP/SL 조합은 동일 진입을 서로 다른 청산 규칙으로 추적한 결과일 수 있다.

- 진입 표본 수: 고유 `trade_id` 개수
- 종목 표본 수: 고유 `stock_code` 개수
- 조건별 전략 결과 표본: `trade_id + TP + SL`
- 시간 경로 표본: trade_id 및 관측 horizon의 조합

예를 들어 30개의 trade_id × 169개 TP/SL 결과는 5,070개의 독립 거래가 아니다. 주 결과표에서 행 수와 고유 거래 수를 모두 분리해 표기한다. 같은 종목 재진입과 동일 날짜 표본의 군집도 표시한다.

## 4. 결과 분석 순서

### A. 전략 평가

1. 코드/설계상 각 전략의 ON/OFF와 진입 조건, 진입 가능 시간, 관찰 종료 조건을 확인한다.
2. 종목 후보 수, 전략별 SIGNAL 수, FILL 수, NO_FILL/REJECT 수를 별도 집계한다.
3. SNAPSHOT류는 horizon별 READY/WATCH/REJECT_RESET/DATA_UNAVAILABLE/ENTRY_TIME_CLOSED 수와 사유를 비교한다.
4. SIGNAL→FILL→paper grid 생성→알림 흐름을 원장과 시간순으로 대조한다.
5. 0건은 성과 0으로 표현하지 않는다. “평가 표본 없음”으로 기록하고 조건 부재와 데이터 미수집을 분리한다.
6. BASE/FIRST_75 등 비교군은 같은 대표 TP/SL을 적용해 비교한다. 대표 조합과 선택 이유를 밝힌다.
7. 모든 TP/SL을 탐색한 결과의 최고 조합을 성과 근거로 선택하지 않는다. 다중비교·사후선택 편향임을 밝힌다.
8. 표본 수, net 승률, 평균·중앙값 수익, Profit Factor, TP/SL/TIME_EXIT 비중, MFE/MAE, 보유시간, 날짜별 안정성을 함께 본다.
9. 날짜·종목이 같은 비교 표본은 독립으로 과대평가하지 않는다. 표본이 적으면 결론을 유보한다.
10. 전략별 결론을 “유지 관찰 / 수정 근거 부족 / 추가 수집 / 중단 검토”처럼 증거 수준에 맞춰 적는다. 한 거래일 성과만으로 실전 승격하지 않는다.

### B. 순수익과 비용

- gross 수익과 net 추정 수익을 함께 표시한다.
- 비용률은 해당 실행 코드/인수인계서의 가정을 확인한다. v2.3.0 예시는 왕복 0.24%다.
- net 승률은 net 수익 > 0인 고유 trade_id 기준으로 계산한다.
- Net PF = 양수 net 결과 합 / 음수 net 결과 절대값 합.
- 평균값은 거래별 산술 평균인지, 자본가중인지 명확히 적는다.
- 비용·슬리피지 정보가 추정값이면 실제 계좌 확정 수익으로 부르지 않는다.
- `paper_trades` 그리드 각 행을 독립 거래로 계산하지 않는다.

### C. 데이터 수집 성능

다음 항목을 점검한다.

- 첫 시작시각, 검증 실패·재시작 횟수, 토큰 발급, WebSocket 로그인·재연결
- scanner 신호 수와 시장별/세션별 스캔 횟수
- API 요청·성공·실패·429·timeout·처리시간
- 스캔 실제 주기 분포(평균보다 중앙값·상위 분위수·최대값을 함께 표시)
- WebSocket tick 수, sequence gap, 완료봉 수, 유효봉 수, 누락·복구 구간
- candidate 등록시각, 유효 관찰 시작시각, L1/snapshot 생성시각
- 프로그램 중단·정상 종료·flush·checkpoint 성공 여부
- 계획 출력 파일 대비 실제 생성 파일 누락

유효 데이터 비율은 분자(유효봉)와 분모(평가대상 완료봉)가 의미상 정확한지 먼저 검증한다. 계측값이 논리적으로 모순되면 해당 지표를 사용하지 않고 결함으로 보고한다. 종료 시점의 마지막 state만으로 장중 전체 데이터 공백을 추론하지 않는다.

### D. 부하 정도

- tick queue backlog와 최대값
- writer queue backlog와 최대값
- open_strategy_count와 후보 수
- compute_drop, coalesce, write_full, P1 복구 성공/실패
- save_failures, halted, shutdown flush 잔여량
- CPU/메모리가 있으면 시간대별 추세와 지속시간
- 로그 증가량과 결과 파일 크기

`open_strategy_count`는 grid 조합 수일 수 있으므로 독립 포지션 수로 오해하지 않는다. 순간 최대값과 지속 적체를 구분하며, queue_max의 단일 spike만으로 과부하라 단정하지 않는다. 반대로 샘플 간격이 길면 잠깐 발생한 피크를 놓칠 수 있다고 쓴다.

## 5. 신규 전략 Telegram 미수신 진단

Telegram 부재는 단계별로 진단하고 “텔레그램 오류”로 바로 단정하지 않는다.

| SIGNAL | FILL | outbox | 판정 |
|---:|---:|---|---|
| 0 | 0 | 없음 | 신호 자체가 생성되지 않음. 조건·후보·데이터 품질부터 확인 |
| 있음 | 0 | 없음 | 미체결. pending 만료/다음 tick/세션/데이터 공백/entry reject 사유 확인 |
| 있음 | 있음 | 없음 | 알림 예약 경로 누락 가능성. 진입 코드와 알림 queue 호출 검토 |
| 있음 | 있음 | PENDING | worker 대기·checkpoint·실행 종료 여부 확인 |
| 있음 | 있음 | DELIVERY_UNKNOWN | 전송 응답 불명. 중복 발송 여부를 먼저 방지하고 Telegram 상태 확인 |
| 있음 | 있음 | SENT | 프로그램이 전송 성공 응답을 기록했음. 단말 표시/채팅방 설정은 별도 확인 |

`SENT`는 응답 성공 기록이지 사용자가 Telegram UI에서 실제 메시지를 봤다는 독립 증명은 아니다. 데이터 원장에 신규 SIGNAL/FILL CSV가 빠졌다면 state JSON/outbox와 system 로그를 교차검증하고, 확정 불가 사유를 남긴다.

## 6. 표준 보고서 구조

매 실행 분석은 다음 구조로 작성한다.

1. 대상 버전·실행일·데이터 경로·실행 모드
2. 5줄 이내 요약과 판단 신뢰도
3. **전략 평가**: 전략별 후보/SIGNAL/FILL/표본 수·수익·비용·결론
4. **데이터 수집 성능**: 시작·API·WebSocket·봉·누락·파일 완전성
5. **부하 정도**: queue·drop·writer·메모리·종료 drain
6. **신규 알림 원인**: SIGNAL→FILL→outbox 단계표
7. **데이터/계측 한계** 및 확인 불가 항목
8. 다음 실행의 우선 조치
9. 집계식·비용 가정·사용 파일 목록

결론은 확인된 사실, 해석, 가설을 명시적으로 구분한다. 숫자에는 단위·분모·비용 포함 여부를 붙이고, 표본 부족은 숨기지 않는다.

## 7. v2.3.0 분석에서 확인된 지침 적용 사례

- 5,070 paper rows를 5,070건으로 세지 않고 30개 고유 trade_id × 169-grid로 해석했다.
- 신규 strategy 신호가 0이라 전략 성과를 계산하지 않았다.
- 기존 전략 outbox의 `SENT`와 신규 전략 signal 0을 분리해 텔레그램 미수신 원인을 진단했다.
- `bars == invalid_bars` 계측 모순과 후보 종료 DATA_UNAVAILABLE 때문에 유효봉률·신규전략 무신호의 근본 원인을 확정하지 않았다.
- MAIN 외 세션이 섞인 scan 간격 통계임을 밝혀 다음 분석에서 MAIN 구간을 따로 측정하도록 했다.
