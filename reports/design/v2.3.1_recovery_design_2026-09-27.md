# v2.3.1 신규전략 데이터 경로 복구 설계 — 2026-09-27

## 1. 목적

v2.3.0 장중 실행에서 신규 STRUCTURE/SNAPSHOT 전략이 평가되지 못한 원인을 수정한다. 이번 버전은 전략 조건을 조정하는 버전이 아니라 가격 tick→1분봉→구조 상태→SNAPSHOT→SIGNAL/FILL→알림 원장을 복구하는 버전이다.

- 부모 코드: 삭제 전 검증본 `f6cb94bf6cd0aaefc290e3168912ee1526d5c7d8`의 `code/releases/027_260923_v2.3.0.ipynb`
- 제안 버전: `v2.3.1`
- 기본 실행모드: `EXECUTION_MODE="RESEARCH"`
- 실제·키움 모의 주문: 0건 유지
- STRUCTURE/SNAPSHOT 조건, 30~240분 horizon, 신규전략 63-grid: 변경하지 않음
- BASE/FIRST_75와 기존 169-grid, ETF, 주문 안전 경로: 그대로 보존

## 2. 확인된 결함

### 2.1 모든 완료봉 무효화

`v220_on_tick()`은 매 tick마다 `v230_init_candidate()`를 호출한다. 초기화 함수의 defaults에 `next_bar_partial=True`가 들어 있어 새 봉 생성 시 `pop()`으로 소비한 플래그가 같은 분의 다음 tick에서 다시 생성된다. 다음 분봉 생성 때마다 해당 플래그가 소비되어 모든 봉이 `valid=False`가 됐다.

실행 결과는 완료봉 9,526개, 무효봉 9,526개이며 후보 17개 모두 `high=null`, `L1=null`, `history=0`이다.

### 2.2 후보별 1분봉 원장 누락

v2.3.0이 `v220_complete_bar()`를 덮어쓰면서 v2.2.0에 있던 `pullback_candidate_1m_*.csv` 저장 블록을 포함하지 않았다. 따라서 사용자가 생성된 파일을 모두 업로드했어도 후보별 OHLC를 이용한 사후 재평가가 불가능했다.

### 2.3 검증 공백

기존 v2.3.0 fixture는 `valid=True`인 완성봉을 직접 `v220_complete_bar()`에 전달했다. 실제 실행 경로인 동일 분 내 복수 tick, 후보 생성 중간봉, 분 전환, 재접속·sequence gap을 통과하지 않아 플래그 재생성 오류를 발견하지 못했다.

## 3. 상태관리 수정

### 3.1 초기 생성과 스키마 보완 분리

현재 `v230_init_candidate()`의 역할을 두 함수로 분리한다.

1. `v230_create_candidate_state()`
   - 신규 후보 생성 때 한 번만 호출
   - `needs_partial_bar=True` 설정
   - H/L1/R1/B/L2, history, pending 등 신규 상태 생성

2. `v230_ensure_candidate_schema()`
   - 복구한 구버전 상태의 누락 필드만 보완
   - 매 tick 호출 가능하도록 멱등성 보장
   - 소비되는 일회성 플래그를 다시 `True`로 만들지 않음

### 3.2 부분봉 플래그 소비

`pop()`으로 키를 지우지 않고 값만 전환한다.

```python
partial = bool(c.get("needs_partial_bar", False))
partial = partial or minute <= candidate_start_minute
c["needs_partial_bar"] = False
```

- 신규 후보: 등록된 현재 분만 부분봉
- 다음 완전한 분부터 유효봉
- 동일 분의 추가 tick: 플래그를 되살리지 않음
- 당일 복구·WebSocket 재접속: 명시적으로 `needs_partial_bar=True` 설정
- 일반 스키마 보완: 현재 값을 변경하지 않음

### 3.3 공백 후 회복

무효 원인을 상태에 명시한다.

- `INITIAL_PARTIAL`
- `CANDIDATE_START_PARTIAL`
- `SEQUENCE_GAP`
- `NON_CONTIGUOUS_MINUTE`
- `POST_RECONNECT_PARTIAL`
- `WRITE_UNAVAILABLE`

공백이 발생한 봉만 무효화하고, 다음 연속 완료봉부터 history를 다시 쌓는다. 무효봉을 지지·돌파·SNAPSHOT 판정에 사용하지 않는다.

## 4. 원장 복구

### 4.1 후보별 완료봉 원장

`pullback_candidate_1m_v231.csv`를 복구하고 모든 완료봉을 저장한다. 무효봉도 원인 분석을 위해 저장한다.

필수 컬럼:

- 날짜·버전·종목코드·종목명
- 후보 출처·후보 등록시각
- minute, OHLC, volume, tick_count
- `bar_valid`, `invalid_reason`
- sequence 시작/종료값과 gap 여부
- current_state
- high/H 시각
- L1/R1/B/L2 값과 시각
- support_touch_count
- data_status, recovery_status
- 처리 지연시간

원장 저장은 구조 조건의 조기 반환보다 먼저 실행한다.

### 4.2 빈 원장도 생성

프로그램 시작 시 다음 파일을 헤더만이라도 생성한다.

- `pullback_candidate_1m_v231.csv`
- `pullback_structure_signals_v231.csv`
- `pullback_snapshot_evaluations_v231.csv`
- `pullback_metrics_v231.csv`

파일이 없다는 사실을 신호 0건과 구분할 수 있게 한다.

### 4.3 무신호 사유 집계

종료 요약에 전략별로 다음 단계를 기록한다.

- 후보 수
- 유효봉이 한 개 이상인 후보 수
- H 생성 수
- L1 생성 수
- STRUCTURE_READY 수
- SNAPSHOT WATCH/READY/REJECT/DATA_UNAVAILABLE 수
- SIGNAL/FILL 수
- 단계별 0건 사유

## 5. 자동 이상 감지

신규전략 전용 health gate를 추가한다. BASE/FIRST_75 연구와 주문 안전 경로에는 영향을 주지 않는다.

후보가 생긴 뒤 5분 또는 완료봉 3개가 지난 시점에 다음을 검사한다.

- `bars > 0`
- `valid_bars > 0`
- `invalid_bars < bars`
- 유효봉이 있는 후보의 `high is not None`
- minute 원장 write 성공

실패 시:

1. STRUCTURE/SNAPSHOT 신규 신호 생성만 중단
2. BASE/FIRST_75와 기존 포지션 추적·청산은 계속
3. 원인, counts, 종목코드, 마지막 무효 사유를 로그와 Telegram으로 1회 즉시 알림
4. 같은 오류의 반복 알림은 억제
5. 종료 요약에 health gate 실패를 남김

## 6. 검증 설계

### 6.1 실제 tick→분봉 회귀검증

완성봉을 직접 주입하는 검증만으로 승인하지 않는다. `handle_realtime_price`와 동일한 입력 형태에서 `v220_on_tick`까지 실행한다.

필수 fixture:

1. 후보 생성 중간 분에 복수 tick
   - 최초 부분봉 1개만 무효
   - 다음 2개 이상의 분봉은 유효
   - `bars > invalid_bars`, history 증가, high 생성

2. 같은 분 복수 tick
   - 플래그가 다시 생성되지 않음
   - OHLC와 tick_count 정확

3. 분당 tick 1개
   - 연속 분이면 유효봉 생성

4. 한 분 무수신
   - 다음 봉 `NON_CONTIGUOUS_MINUTE`
   - 이후 연속봉에서 자동 회복

5. sequence gap
   - 해당 봉만 무효
   - 다음 정상봉부터 회복

6. WebSocket 재접속
   - 재접속 경계 봉을 구조 판단에서 제외
   - 이후 유효봉 재개

7. 재시작 상태 복구
   - 과거 pending 취소
   - 부분봉 1회만 제외
   - 일간 중복키 유지

### 6.2 전략 상태 fixture

- H→1.50% L1→0.40% 반등→R1→0.30% retest→L2→STRUCTURE_READY
- 고정돌파선 B 종가 돌파와 다음 봉 유지
- SNAPSHOT 30~240분 WATCH/READY/REJECT_RESET/DATA_UNAVAILABLE
- 15:10 신규진입 종료와 15:20 평가 종료
- STRUCTURE/SNAPSHOT별 동일 종목·당일 중복차단
- 신규전략 63-grid, 기존전략 169-grid 결정순서

### 6.3 원장 fixture

- 신호 0건이어도 네 파일 존재와 header 검증
- 유효봉·무효봉 행과 invalid_reason 검증
- SIGNAL→FILL→grid→outbox 연결 검증
- writer 실패 시 신규전략만 중단되는지 검증
- 종료 flush 후 queue remaining=0 확인

### 6.4 결함 재주입 검사

검사기 자체가 결함을 잡는지 확인하기 위해 다음 negative control을 실행한다.

- `ensure_candidate_schema()`가 `needs_partial_bar=True`를 매번 다시 넣도록 임시 변경하면 회귀검증이 반드시 실패
- minute 원장 enqueue를 제거하면 필수 파일·행 수 검증이 반드시 실패

### 6.5 기존 경로 보존 검증

- 부모 top-level 정의 보존
- 주문·broker 핵심 함수 동일성 검증
- compile 및 pyflakes 미정의 이름 검사
- clean-process cold-start
- 기본 `run_scanner()` 시작 검증을 토큰 요청 직전까지 실행
- 외부 연결 0건, 실제·모의 BUY/SELL 0건 assertion
- 독립 2차 비판적 리뷰와 최종 회귀검증

## 7. 첫 장중 실행 승인 기준

다음 조건을 모두 만족할 때만 v2.3.1을 장중 연구에 사용한다.

- main에 v2.3.1 전체 notebook과 검증 보고서가 함께 존재
- README/CURRENT와 release 경로 일치
- 실제 tick 경로 fixture PASS
- negative control PASS
- 신규전략 조건·grid 변경 없음 확인
- 시작 후 첫 후보 기준 5분 내 유효봉 발생
- `valid_bars > 0`, `invalid_bars < bars`
- 후보 하나 이상 `high != null`
- candidate minute 원장에 실제 행 생성
- 신규 SIGNAL/SNAPSHOT 파일이 0건이어도 존재
- health gate 오류 알림 없음

첫 5분 검증에 실패하면 당일 신규전략 결과를 성과자료로 사용하지 않는다.

## 8. 저장소 정합성 복구

현재 main에서 삭제된 `code/releases/027_260923_v2.3.0.ipynb`와 문서 참조가 불일치한다. v2.3.1 작성 시 다음을 한 작업으로 처리한다.

1. 삭제 전 v2.3.0 검증본을 부모로 복원
2. 최소 변경으로 v2.3.1 생성
3. build, inspection, regression 보고서 생성
4. `code/releases/028_260928_v2.3.1.ipynb` 저장
5. README와 CURRENT를 v2.3.1로 갱신
6. 최종 검증 PASS 후 main 직접 업로드

## 9. 변경하지 않는 항목

- 신규전략의 가격 조건과 horizon
- BASE/FIRST_75 조건
- 기존 전략 169-grid
- 신규 전략 63-grid
- ETF 연구
- 실제 주문 안전장치와 fallback
- 실행 기본값 RESEARCH
- 실제·키움 모의 주문 차단


## 10. 추가 코드 감사에서 확인한 필수 수정

### 10.1 SNAPSHOT 저장 성공 전 상태 확정 금지

현재 `v230_snapshot()`은 `enqueue_research_rows()`의 반환값을 확인하지 않고 `snapshots_done`을 기록하며, READY이면 신규 신호까지 만들 수 있다. 원장 저장이 실패했는데 진입은 생성되는 불일치가 생길 수 있다.

v2.3.1에서는 다음 순서를 강제한다.

1. SNAPSHOT row P1 저장 또는 복구 spool 성공
2. 저장 성공 확인
3. `snapshot_results`, `snapshots_done`, `snapshot_ready` 갱신
4. DIRECT signal 생성

저장이 실패하면 해당 신규전략을 health halt하고 SIGNAL/FILL을 만들지 않는다.

### 10.2 모든 출력 파일 v2.3.1 분리

pullback 파일만 바꾸지 않고 실행 중 생성되는 모든 결과 파일명을 점검해 `v231`로 분리한다. v2.3.0 결과에 이어쓰기하지 않는다.

- scanner signals/system
- paper trades/decisions/path/policy/post-exit
- pullback state/minute/signals/snapshot/metrics
- 기존 ETF 파일은 스키마가 바뀌지 않더라도 provenance에 실행 버전과 날짜를 보존하고 혼합 여부를 검증

시작 검증에서 active output path 목록을 출력하고 `v230` 잔존 경로가 있으면 실패시킨다. 의도적으로 유지하는 호환 파일은 허용목록과 이유를 명시한다.

### 10.3 손상 상태와 신규 상태 분리

v2.3.0의 후보 상태는 모든 history가 비어 있어 v2.3.1로 복원하면 안 된다.

- 신규 상태 파일 `pullback_state_v231.json` 사용
- state schema를 올리고 `schema`, `strategy_version`, `trading_date`를 검증
- v2.3.0 state 자동 승계 금지
- 같은 날 v2.3.1 재시작만 v2.3.1 state에서 복구
- 호환되지 않는 state는 원본을 보존하고 명확한 오류로 시작 중단

### 10.4 일자 전환 시 누적 계측 초기화

`v220_reset_day()`에서 후보와 신호뿐 아니라 다음도 함께 초기화한다.

- ticks, gaps, bars, valid_bars, invalid_bars
- invalid reason별 counters
- save_failures
- 종목별 tick sequence
- health gate 상태와 중복 알림키
- 마지막 metrics 기록시각

일자별 CSV와 state의 누적값이 전일에서 이어지지 않는지 이틀 연속 가상시계 fixture로 검증한다.

### 10.5 버전 고정 검증 전수 수정

v2.3.0 첫 실행을 막았던 exact version assertion 유형을 다시 전수 검색한다.

- `STRATEGY_VERSION` 허용목록
- 시작 validator
- opening leader validator
- off-hours validator
- 출력 filename·rule version
- Telegram/로그의 하드코딩된 버전 문자열

기본 설정의 실제 `run_scanner()` 시작 경로를 토큰 요청 직전까지 호출해 v2.3.1이 버전 검증에서 중단되지 않는지 확인한다.

### 10.6 활성 함수 정의 감사

부모 호환을 위해 `v220_complete_bar`, `v220_on_tick`, `v220_restore`, `validate_v220_config` 등이 여러 번 정의되어 있다. Python에서는 마지막 정의만 실행되므로 수정 위치가 잘못되면 테스트 코드와 장중 코드가 다른 구현을 사용할 수 있다.

이번 복구에서는 대규모 재구성을 하지 않되 다음을 검증한다.

- 실제 활성 함수의 source line과 코드 hash 기록
- 호출부가 기대한 마지막 정의를 가리키는지 assertion
- 부모 호환 함수와 v2.3.1 활성 함수의 역할을 주석으로 명확히 표시
- 회귀보고서에 활성 정의 목록 첨부

### 10.7 metrics 의미 보강

기존 `bars`와 `invalid_bars` 외에 다음을 추가한다.

- `valid_bars`
- `initial_partial_bars`
- `candidate_start_partial_bars`
- `sequence_gap_bars`
- `non_contiguous_bars`
- `post_reconnect_partial_bars`
- `minute_write_failures`
- `snapshot_write_failures`
- `candidates_with_valid_bar`
- `candidates_with_high`
- `candidates_with_l1`

항상 `bars == valid_bars + invalid_bars`가 성립해야 하며, reason별 무효봉 합도 `invalid_bars`와 일치해야 한다.

### 10.8 장중 조기 이상 알림과 종료 요약 분리

장중 health gate 실패는 즉시 1회 알림을 보내고 신규 세 전략을 중단한다. 종료 시에는 별도 요약으로 다음을 보낸다.

- 신규전략 평가 가능/불가능
- 후보·유효봉·H·L1·READY·SIGNAL·FILL 단계별 건수
- 무효봉 원인별 건수
- 원장 저장 실패 여부
- health halt 여부와 최초 발생시각

알림 전송 성공 여부도 outbox에 남겨 전략 무신호와 Telegram 장애를 분리한다.

## 11. 수정 우선순위

### P0 — v2.3.1 실행 전 필수

1. 부분봉 플래그 재생성 제거
2. 실제 tick→분봉 회귀검증
3. 후보별 minute 원장 복구
4. SNAPSHOT ledger-first 처리
5. v231 출력·state 완전 분리
6. 버전 assertion과 실제 시작 경로 검증
7. health gate 및 유효봉 자동검사
8. 기존 BASE/FIRST_75·주문 경로 회귀검증

### P1 — 같은 릴리스에 포함

1. 일자 전환 counters 초기화
2. 무효봉 원인별 metrics
3. 빈 원장 header 선생성
4. 활성 중복 정의 감사
5. 단계별 무신호 종료 요약
6. 하드코딩된 v2.2.0/v2.3.0 로그를 runtime 버전 표시로 정리

### 보류

전략 조건, SNAPSHOT 간격, 평가 종료시각, TP/SL grid 변경은 정상 데이터가 다시 수집된 뒤 판단한다.


## 12. 20시 이후 종목별 실제 수익률 독립 수집 셀

### 12.1 목적과 배치

메인 셀 다음에 **당일 종목별 실제 실현손익 조회·누적 저장 전용 셀**을 새로 추가한다.

노트북 셀 순서는 다음과 같다.

1. 실행 설정
2. 메인 스캐너 전체 코드
3. 종목별 실제 수익률 조회·누적 CSV 저장
4. 변수·용어 QUICK REFERENCE
5. CONTINUITY NOTES

셀 3은 메인 스캐너를 시작하거나 주문을 제출하지 않는다. 사용자가 20시 종료 후 수동으로 실행하며, 메인 셀에서 오류가 발생했어도 별도로 실행할 수 있게 구성한다.

### 12.2 조회 자료

정확한 종목별 실현손익을 위해 세 자료를 역할별로 사용한다.

- `ka10077` 당일실현손익상세요청: broker가 계산한 종목별 실현손익·수익률의 주 기준
- `ka10170` 당일매매일지요청: 당일 매입·매도·비용·손익 대조
- `ka10076` 체결요청: 주문번호, 부분체결, 자동·수동 분류의 보조 원장

계산한 매입·매도 금액과 broker 실현손익 자료가 일치하지 않으면 임의로 한쪽을 선택하지 않고 `RECONCILIATION_REQUIRED`로 표시한다.

### 12.3 셀 독립성과 주문 차단

셀 3은 다음 기능을 자체 포함한다.

- 표준 라이브러리와 `requests`, `pandas` import
- 현재 폴더의 `.env` 로드
- 접근토큰 발급
- 조회 전용 POST와 연속조회
- 종목별 집계와 broker 손익 대조
- 기존 CSV 병합·중복 제거·원자 저장
- 실행 결과 요약

메인 셀 함수, thread, queue, WebSocket, 전역상태가 없어도 실행 가능해야 한다. 셀 1을 실행하지 않은 새 커널에서도 셀 3 하나만으로 동작하는 clean-process 검증을 수행한다.

주문 API `kt10000`, `kt10001`, `kt10002`, `kt10003` 호출은 코드에 포함하지 않으며, 테스트에서 주문 호출 0건을 확인한다.

### 12.4 저장 단위와 파일

기본 저장 파일:

`broker_daily_stock_pnl.csv`

한 행은 **거래일 + 종목코드 한 개**다. 같은 종목의 분할매수, 부분체결, 부분매도, 여러 번의 매수·매도를 모두 당일 한 행으로 합친다.

중복키:

`trade_date + stock_code`

같은 날 셀을 다시 실행하면 행을 추가하지 않고 broker 최신 확정값으로 해당 종목 행을 갱신한다. 다른 날짜는 계속 누적한다.

### 12.5 필수 컬럼

#### 식별·분류

- `trade_date`
- `stock_code`
- `stock_name`
- `classification`
- `matched_auto_order_count`
- `unmatched_order_count`
- `queried_at`
- `collector_version`
- `reconciliation_status`

#### 매입

- `buy_order_count`
- `buy_fill_count`
- `buy_qty`
- `avg_buy_price`
- `buy_amount`

#### 매도

- `sell_order_count`
- `sell_fill_count`
- `sell_qty`
- `avg_sell_price`
- `sell_amount`

#### 손익

- `realized_qty`
- `broker_cost_basis`
- `gross_profit`
- `buy_commission`
- `sell_commission`
- `tax`
- `total_cost`
- `net_profit`
- `net_return_pct`
- `broker_reported_profit`
- `broker_reported_return_pct`

#### 잔여 상태

- `remaining_qty`
- `position_status`
- `first_buy_time`
- `last_sell_time`

계좌번호·앱키·토큰은 저장하지 않는다.

### 12.6 집계 방법

부분체결과 부분매도는 체결금액 가중평균으로 합친다.

```text
평균매입가 = 총 매입 체결금액 / 총 매입수량
평균매도가 = 총 매도 체결금액 / 총 매도수량
```

수익금과 수익률은 broker가 제공하는 실현손익 값을 주 기준으로 사용한다.

```text
순수익금 = 실현매도금액 - 실현원가 - 매수수수료 - 매도수수료 - 세금
순수익률 = 순수익금 / 실현원가 × 100
```

- 일부만 매도했으면 매도한 수량에 해당하는 실현손익만 기록
- 남은 수량은 `remaining_qty`에 표시
- 전일부터 보유한 종목을 당일 매도한 경우 당일 매수금액만으로 원가를 추정하지 않고 broker 원가를 사용
- 같은 종목을 당일 여러 차례 왕복매매해도 종목별 총계 한 행으로 합침
- 조회 API에 이미 비용 후 실현손익이 있으면 이를 보존하고 자체 계산값과 비교
- 비용 항목을 API가 분리 제공하지 않으면 빈값으로 두고 추정값을 넣지 않음

### 12.7 자동·수동 분류

체결 주문번호를 프로그램의 `live_orders_v231.csv`와 대조한다.

- 모든 관련 체결이 프로그램 주문번호와 일치: `AUTO_CONFIRMED`
- 일치 주문과 미일치 주문이 같은 종목에 함께 있음: `MIXED`
- 모든 체결이 미일치: `MANUAL_OR_UNMATCHED`
- 주문번호가 불완전하거나 프로그램 원장 손상: `UNKNOWN`

`MANUAL_OR_UNMATCHED`를 확정 수동매매로 부르지 않는다. 프로그램이 주문번호를 저장하기 전에 종료됐거나 다른 자동 프로그램이 주문했을 가능성이 있기 때문이다.

자동 주문이 확인되면 별도 컬럼 또는 보조 원장에서 전략명, 진입·청산 사유, 프로그램 버전을 연결한다. 한 종목에 여러 전략이나 수동 주문이 섞이면 종목 요약은 `MIXED`로 표시한다.

### 12.8 연속조회와 완전성

조회 전용 요청 함수는 응답 header의 continuation 정보를 보존해 마지막 페이지까지 가져온다.

- 다음 페이지 key 반복 방지
- 최대 페이지 안전상한
- 페이지별 행 수와 총 행 수 기록
- 중간 페이지 실패 시 기존 누적 CSV를 갱신하지 않음
- 3개 API 중 일부만 성공하면 `PARTIAL`로 표시하고 완료본으로 확정하지 않음
- 체결 종목 집합과 실현손익 종목 집합 불일치 검사

### 12.9 실행 결과 표시

```text
조회일: YYYY-MM-DD
거래 종목: N개
매수 총액: N원
매도 총액: N원
순수익금: N원
비용 후 수익률: N%
AUTO_CONFIRMED: N종목
MIXED: N종목
MANUAL_OR_UNMATCHED: N종목
대조 필요: N종목
신규 추가/갱신: N/N
저장 파일: broker_daily_stock_pnl.csv
상태: COMPLETE
```

0종목도 API 조회 성공이면 정상 결과로 처리한다. 조회 실패와 0건을 구분한다.

### 12.10 검증

1. 메인 셀을 실행하지 않은 새 커널에서 셀 3 단독 실행
2. 다중 페이지 연속조회
3. 같은 날 두 번 실행해 날짜+종목 중복 없음
4. 분할매수 가중평균
5. 부분매도 가중평균과 잔여수량
6. 전일 보유분 당일 매도의 broker 원가 사용
7. 동일 종목 여러 차례 왕복매매의 일일 합계
8. AUTO/MIXED/MANUAL_OR_UNMATCHED/UNKNOWN 분류
9. broker 손익과 자체 계산 대조
10. 중간 API 실패 시 기존 CSV 불변
11. 주문 API 호출 0건
12. 토큰·앱키·계좌번호 비저장
13. CSV가 Excel에서 열려 있을 때 명확한 저장 실패 안내

### 12.11 운영 방법

- 권장 실행시각: 메인 코드가 정상 종료한 20:00 이후, 날짜가 바뀌기 전
- 실행 방식: 셀 3만 수동 실행
- 같은 날 여러 번 실행 가능하며 종목별 행을 갱신
- 최초 실제 사용일에는 CSV의 종목·수량·매입가·매도가·손익을 영웅문 당일매매/실현손익 화면과 대조
- 영웅문과 불일치하면 해당 행을 분석에 사용하지 않고 `RECONCILIATION_REQUIRED`로 유지
