# v2.3.0 실행 결과 분석 — 2026-09-23

- 분석일: 2026-09-26
- 대상 실행: 2026-09-23
- 저장 경로: `data/2.3.0(260923)/`
- 분석 구분: 전략 평가 · 데이터 수집 성능 · 부하 정도
- 분석 기준: 저장소의 실행 CSV/JSON과 v2.3.0 설계·코드 기록
- 실행 모드: RESEARCH. 실제·키움 모의 주문은 실행하지 않음

## 요약

1. 신규 STRUCTURE/SNAPSHOT 전략은 가상진입과 기록된 SIGNAL 모두 0건이다. 따라서 이번 하루 결과로 신규전략의 성과를 평가할 수 없다.
2. 텔레그램 전송 실패가 신규알림 부재의 직접 원인이라는 증거는 없다. 상태파일의 기존 BASE/FIRST_75 알림은 30건 모두 `SENT`였고, 신규 전략 신호·체결은 0건이다.
3. 신규전략이 신호를 만들지 못한 정확한 단계는 판별 불가다. 필수 SIGNAL/SNAPSHOT 원장이 저장소에 없고, 종료 상태의 후보 17개는 모두 `DATA_UNAVAILABLE`·빈 history로 남았다. 봉 수집 지표도 `invalid_bars == bars`로 기록되어 신뢰할 수 없다.
4. 연구 큐·기록 writer 부하는 낮았다. 반면 전체 로그의 시장 스캔 간격은 중앙값 약 19초로, 설계상 15초 주기보다 느렸다.
5. 기존 기준 전략은 한 거래일의 소표본에서 약했다. 대표 조합 T250_S150의 비용 차감 평균은 BASE −0.543%, PRE_HISTORY(FIRST_75 계통) −0.406%였다. 실전 판단이나 전략 폐기 결론을 내릴 표본은 아니다.

## 1. 전략 평가

### 신규 v2.3.0 전략

| 확인 항목 | 결과 |
|---|---:|
| BASE 후보 등록 | 17종목 |
| FIRST_75 후보 등록 | 6종목 |
| STRUCTURE 신규 SIGNAL / paper FILL | 0 / 0 |
| SNAPSHOT_DIRECT 신규 SIGNAL / paper FILL | 0 / 0 |
| SNAPSHOT_RECLAIM 신규 SIGNAL / paper FILL | 0 / 0 |
| 신규전략 알림 outbox 항목 | 0 |
| 저장된 신규 SIGNAL 원장 | `pullback_structure_signals_v230.csv` 없음 |
| 저장된 SNAPSHOT 평가 원장 | `pullback_snapshot_evaluations_v230.csv` 없음 |

직렬화된 종료 상태에서도 전략별 신호 수는 BASE 17, FIRST_75 6, SUPPORT/RECLAIM·STRUCTURE·SNAPSHOT 신규전략은 각각 0이었다. 알림 outbox 30건은 모두 `SENT`이며 미확인 전송은 없다. 따라서 확인 가능한 사실은 **새 전략 신호/가상진입이 저장되지 않았다**는 것이다. “신호가 있었는데 Telegram만 누락됐다”고 볼 근거는 없다.

코드 설계상 개별 신규전략 알림은 paper FILL 뒤 outbox에 등록된다. 즉 알림이 오려면 전략 SIGNAL, 다음 정상 tick의 FILL, outbox 등록, worker 전송이 순서대로 성공해야 한다. 이번 자료에서는 첫 단계의 신규 SIGNAL이 0건이라 개별 알림도 만들어지지 않았다.

### 신규전략 무신호의 해석 한계

종료 시 후보 17개 모두 `data_status=DATA_UNAVAILABLE`, `history=0`, `L1=null`로 기록되어 있고 상태 metrics의 sequence gap 누계도 17이다. 이 값들은 후보별로 구조를 최종 복원할 수 없었다는 점을 보여준다. 다만 종료 상태만으로 장중 내내 데이터가 끊겼다고 단정할 수는 없다. 저장된 상태가 종료 직전 마지막 공백으로 갱신됐을 가능성이 있기 때문이다.

더구나 설계상 L1은 후보 고점 H에서 1.50% 이상 눌려야 생긴다. 전용 snapshot 평가 원장이 없으므로 다음 원인을 구분할 수 없다.

- 가격이 L1 조건까지 눌리지 않아 후보가 만들어지지 않음
- 완료 1분봉 공백·복구로 구조 판단이 제한됨
- L1 이후 SNAPSHOT 평가가 WATCH/REJECT/DATA_UNAVAILABLE로 끝남
- 그 밖의 실행 경로에서 평가·원장 저장이 생략됨

다음 실행에서는 전용 SIGNAL 원장과 SNAPSHOT 평가 원장을 반드시 함께 저장해야 이 단계를 판별할 수 있다.

### 기존 기준 전략 참고 결과

`paper_trades_v230.csv`는 총 5,070행이지만 5,070개 독립 거래가 아니다. 고유 trade_id 30개 각각에 기존 169개 TP/SL 조합이 생성된 결과다. 여기서는 비교 일관성을 위해 대표 기준 `T250_S150`만 집계하고, 왕복 추정비용 0.24%p를 각 결과에서 차감했다.

| 진입 계통 | 고유 진입 | TP / SL / 시간청산 | 비용 차감 승률 | 평균 gross | 평균 net | Net PF |
|---|---:|---:|---:|---:|---:|---:|
| BASE | 24 | 5 / 14 / 5 | 8/24 (33.3%) | −0.303% | −0.543% | 0.49 |
| PRE_HISTORY (FIRST_75 계통) | 6 | 2 / 4 / 0 | 2/6 (33.3%) | −0.166% | −0.406% | 0.66 |

- 17개 후보 종목과 30개 진입은 동일한 독립 표본이 아니다. 같은 종목의 재진입이 포함되고 PRE_HISTORY는 BASE와 종목·시각이 겹친다.
- 평균 수익률은 거래별 산술 평균이다. 복리 수익률이나 실제 수수료 정산값이 아니다.
- 하루, 소수의 종목, 24/6 진입만으로 BASE/FIRST_75를 수정·중단할 근거는 부족하다.
- 신규 세 전략은 진입 0건이므로 성과·비용 후 기대값·Profit Factor를 계산할 수 없다.

## 2. 데이터 수집 성능

### 정상적으로 확인된 항목

- 시스템 로그는 07:35:29부터 20:00:17까지 실행을 기록한다. 이 시간대에 700,407 ticks, 후보 17종목, 9,526 bars가 metrics에 기록됐다.
- 07:35:30 키움 토큰 발급 성공, 07:35:32 WebSocket 로그인 성공. 07:53에 한 차례 WebSocket 정상 종료 후 재연결과 로그인이 기록됐다.
- 종료 시 연구 writer의 remaining=0, failures=0, dropped_P2=0. 정상 종료 로그가 있다.
- 저장 상태의 기존 전략 알림 outbox는 30건 모두 `SENT`, `DELIVERY_UNKNOWN`은 0이다.

### 시작 지연과 스캔 주기

시스템 CSV에는 07:25:29와 07:35:29 두 번의 시작 배너가 있다. 토큰 발급은 두 번째 배너 후에야 기록된다. 이전 화면의 버전 고정 assertion 오류와 시간상 맞물리므로 첫 실행은 시작 검증 단계에서 멈춘 뒤 재실행한 것으로 보인다. 첫 실패 원인은 화면에서 확인되지만, CSV 자체에는 traceback이 없다.

시스템 로그의 `[SCAN 품질]` 2,176개 기록을 모두 보면 처리시간은 중앙값 약 19초, 최대 28.1초였다(전체 NXT/MAIN/시간외 스캔 포함). 설계 기준 15초보다 실제 처리주기가 길어질 수 있다. 장초반 신호 포착에서는 다음 실행도 MAIN 구간만 따로 집계해 지연 분포를 비교해야 한다.

KA10001 품질 로그는 50,874회 요청·50,863회 성공·11회 실패로 합산됐다. 그중 429 5회, timeout 4회가 기록됐다. 오류 비율은 작지만 호출은 대량이며 429/timeout이 재발하는지 계속 확인할 필요가 있다.

### 데이터 무결성 이슈

1. 저장소 폴더에 `pullback_structure_signals_v230.csv`와 `pullback_snapshot_evaluations_v230.csv`가 없다. 신규 전략 평가의 핵심 근거가 빠졌다.
2. `pullback_metrics_v230.csv`는 684개 샘플 모두 `bars`와 `invalid_bars` 값이 같다. 코드의 의미상 `invalid_bars`는 무효봉일 때만 증가해야 하므로 이 비율을 실제 무효봉률로 해석할 수 없다. 계측 변수, CSV writer schema, 업로드 원본을 다음 검증에서 대조해야 한다.
3. 종료 상태의 후보 17개 모두 history가 비어 있고 데이터 공백 상태다. 장중 유효 데이터의 지속시간·공백 구간을 평가할 후보별 minute 원장이 필요하다.
4. 현재 main 최신 커밋 `cfe9966058d6c779166297fff6981ae46230c0a7`에는 데이터 파일들이 추가됐지만 `code/releases/027_260923_v2.3.0.ipynb`가 삭제(removed)로 기록돼 있다. README/CURRENT는 여전히 해당 release를 가리킨다. 분석용 코드는 삭제 전 커밋 `f6cb94bf6cd0aaefc290e3168912ee1526d5c7d8`의 파일을 기준으로 대조했으며, 저장소의 현재 코드 경로와 문서 상태가 서로 맞지 않는다.

## 3. 부하 정도

| 계측값 | 관측 결과 | 해석 |
|---|---:|---|
| 처리 ticks | 700,407 | 하루 전체 기록 |
| 후보 수 최대 | 17 | 후보 추적 규모 |
| open strategy 조합 최대 | 1,033 | 독립 포지션 수가 아니라 paper grid 조합 수 |
| metrics tick queue 최대 | 5 | 낮은 잔량 |
| writer queue 최대 | 0 | 기록 writer 적체 관측 없음 |
| queue metrics 샘플 수 | 684 | 07:35~19:59 |
| 60초 부하 요약 | 635회 | compute_drop/coalesce/write_full/P1복구 실패 모두 0 |
| system 로그 queue_max 최대 | 9 | 일시적 소량 대기 |
| 연구 save failure | 0 | 기록 실패 지표 |
| 종료 writer remaining / failures | 0 / 0 | 정상 drain |

관측된 연구 처리·파일 writer 부하는 낮았고 queue overflow/drop 증거도 없다. 다만 scan cycle 지연은 연구 queue 부하와 별개의 문제다. 또한 `invalid_bars` 집계 오류와 후보 최종 DATA_UNAVAILABLE 때문에 “수집된 모든 가격 데이터가 전략 판단에 충분히 유효했다”고 결론내릴 수 없다.

## 다음 실행 전 우선 조치

1. GitHub main에서 삭제된 v2.3.0 release 파일과 README/CURRENT의 경로를 정합화한다.
2. 실행 결과 업로드 시 신규 SIGNAL·SNAPSHOT 원장 파일이 생성됐는지 체크리스트로 확인한다. 0건일 때도 헤더만 있는 빈 파일을 남기거나 명시적 `NO_SIGNAL` 종료 요약을 기록한다.
3. metrics에서 `bars`와 `invalid_bars` 누적 로직/CSV header를 수정 또는 독립 검증하고, bars>0·invalid_bars=0 fixture와 invalid bar 1건 fixture를 각각 대조한다.
4. 후보별 유효봉 수, gap 시각·사유, L1 생성/무효화, 각 SNAPSHOT 판정 및 이유를 기록해 후보 lifecycle을 재구성 가능하게 한다.
5. 다음 실행에서는 MAIN 스캔 주기·429/timeout·WebSocket 재연결 후 후보 복구를 계속 확인한다. 표본이 모이기 전 신규전략 조건을 완화하지 않는다.

## 계산 방법과 주의

- 고유 진입 수는 `trade_id` 기준으로 센다. TP/SL 조합별 행 수와 혼동하지 않는다.
- 기준 결과는 `entry_mode + TP + SL`로 필터링하고, 비용 차감 수익은 `return_rate − 0.24%p`로 계산했다.
- Net PF는 비용 차감 수익의 양수 합을 음수 수익 절대값 합으로 나눴다.
- net 승률은 비용 차감 후 수익이 0% 초과인 진입 비율이다.
- 신규 전략 0건은 성과가 0%라는 뜻이 아니라 평가 표본이 없다는 뜻이다.
- 본 분석은 가상매매 기록이다. 실제 broker 체결·계좌 수익의 증거로 보지 않는다.

## 참고

- [GitHub 실행 데이터](../../data/2.3.0(260923)/)
- [v2.3.0 설계 인수인계서](../../handoff/2026-09-22/v2.2.0_to_v2.3.0_STRUCTURE_SNAPSHOT_최종인수인계서_2026-09-22.md)
- [분석 지침](../../DATA_ANALYSIS_GUIDE.md)
